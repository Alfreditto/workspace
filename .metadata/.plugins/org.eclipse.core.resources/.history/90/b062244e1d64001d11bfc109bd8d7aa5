package com.ifpvg.tema2;

import java.sql.CallableStatement;
import java.sql.Connection;
import java.sql.DriverManager;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Statement;
import java.sql.Types;

public class NominaTodos {
	public static void main(String[] args) {
		try {
			Class.forName("com.mysql.jdbc.Driver");
			Connection conexion = DriverManager.getConnection("jdbc:mysql://localhost/ejemplo", "root", "");
			Statement sentencia = conexion.createStatement();
		Double salario = Double.parseDouble(args[0]);
		Double comision = Double.parseDouble(args[1]);
		int irpf = Integer.parseInt(args[2]);

		String sql = "select apellido, salario, comision, nomina(salario, comision, 20) as nomina from empleados;";
		ResultSet resul = sentencia.executeQuery(sql);
		while (resul.next()) {
			System.out.println("Empleado: " + resul.getString(0) + ", Salario: " + resul.getDouble(1) + ", Comision: " + resul.getDouble(2) + ", Nomina: " + resul.getDouble(3));
		}

		} catch (ClassNotFoundException | SQLException e) {
			// TODO Auto-generated catch block
			e.printStackTrace();
		}

	}
}
